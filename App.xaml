<Application x:Class="TB_Browser.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- 🎨 Colors -->
            <SolidColorBrush x:Key="BgBrush" Color="#1e1e1e"/>
            <SolidColorBrush x:Key="SurfaceBrush" Color="#2b2b2b"/>
            <SolidColorBrush x:Key="HoverBrush" Color="#3a3a3a"/>
            <SolidColorBrush x:Key="ActiveBrush" Color="#1e1e1e"/>
            <SolidColorBrush x:Key="TextBrush" Color="#f0f0f0"/>
            <SolidColorBrush x:Key="TextDimBrush" Color="#a0a0a0"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#60cdff"/>

            <!-- 🔘 Base Button Style (Chrome-like) -->
            <Style x:Key="IconBtn" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 📑 Tab Button Style -->
            <Style x:Key="TabBtn" TargetType="Button">
                <Setter Property="MinWidth" Value="120"/>
                <Setter Property="MaxWidth" Value="220"/>
                <Setter Property="Height" Value="34"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border" Background="{TemplateBinding Background}" 
                                    CornerRadius="4 4 0 0" Padding="12,0,8,0"
                                    BorderBrush="{StaticResource BgBrush}" BorderThickness="0 0 0 0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="16"/> <ColumnDefinition Width="*"/> <ColumnDefinition Width="16"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="1" Margin="0,0,4,0" VerticalAlignment="Center" 
                                               Text="{TemplateBinding Content}" FontSize="12"
                                               TextTrimming="CharacterEllipsis"/>
                                    <Button Grid.Column="2" x:Name="CloseBtn" Content="✕" 
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{StaticResource TextDimBrush}"
                                            FontSize="10" Cursor="Hand" Padding="0"
                                            Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                    <Setter TargetName="CloseBtn" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{StaticResource ActiveBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
