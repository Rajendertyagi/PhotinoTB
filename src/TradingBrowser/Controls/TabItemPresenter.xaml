<UserControl
    x:Class="TradingBrowser.Controls.TabItemPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="36"
    d:DesignWidth="200"
    Height="36"
    MinWidth="120"
    MaxWidth="240">

    <Grid x:Name="RootGrid" Background="Transparent" PointerPressed="RootGrid_PointerPressed" ContextRequested="RootGrid_ContextRequested">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="PointerOver">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TabBackground" Storyboard.TargetProperty="Background">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ControlFillColorSecondaryBrush}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Tab Background: Rounded top corners (8,8,0,0) to blend with Mica Alt title bar -->
        <Border x:Name="TabBackground" 
                CornerRadius="8,8,0,0" 
                Background="{ThemeResource ControlFillColorDefaultBrush}" 
                Margin="2,0,2,0"
                Padding="12,0,8,0">
            <Grid ColumnDefinitions="*,Auto">
                
                <!-- Tab Title -->
                <TextBlock x:Name="TitleTextBlock" 
                           Text="{x:Bind Title, Mode=OneWay}" 
                           VerticalAlignment="Center" 
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                           Style="{StaticResource CaptionTextBlockStyle}" 
                           TextTrimming="CharacterEllipsis"
                           Margin="0,0,8,0"/>
                
                <!-- Close Button -->
                <Button x:Name="CloseButton" 
                        Grid.Column="1" 
                        Width="24" Height="24" 
                        Padding="0" 
                        Background="Transparent" 
                        BorderThickness="0"
                        CornerRadius="4"
                        Click="CloseButton_Click"
                        VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE711;" FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
